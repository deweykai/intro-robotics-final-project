# Hello

## Autonomous

Enter autonomous mode from manual mode with `A`. Robot will automatically return to manual mode once navigation finishes.

## Map

Load map data with `L` while in control. `Loading map...` should appear in the console.

Save map data with `S` while in control `Saving map...` should appear in the console.
